{
  "name": "income-ai-assist",
  "version": "1.0.0",
  "private": true,
  "description": "Income AI Assist — native iOS shell (Capacitor) for the on-device mortgage income/DTI estimator PWA",
  "scripts": {
    "sync": "cap sync",
    "ios:add": "cap add ios",
    "ios:sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
